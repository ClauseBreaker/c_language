# 📘 Урок 8. Как работает программа на языке **C**

------

## 🧩 В этом уроке ты узнаешь:

- Что делает каждая строка программы на C
- Что такое `main()`, `#include`, `return`, `printf()`, `\n`, фигурные скобки `{ }`
- Как запускается программа и что делает компьютер
- Почему всё **именно так** и что будет, если что-то убрать

------

## 📦 Вот пример самой первой программы:

```c
#include <stdio.h>  // Подключаем библиотеку для вывода текста

int main(int ac, char **av) {
    printf("Hello, World!\n");  // Показываем текст на экране
    return 0;  // Говорим: "Программа завершилась хорошо"
}
```

------

## 🔍 Разбираем каждую строку по полочкам:

------

### ✅ 1. `#include <stdio.h>`

#### Что это?

Это как **включить инструменты**, которые нам нужны.

- `stdio` — это **стандартный ввод/вывод** (Standard Input/Output).

- `.h` — это **header file** (заголовочный файл).

- `#include` — говорит компилятору:

  > «Подключи эту библиотеку! Я буду использовать `printf()` и другие команды».

📌 Без этой строки `printf()` не сработает.

------

### 🧱 Сравнение:

| В жизни                         | В C                              |
| ------------------------------- | -------------------------------- |
| Взял тетрадь, чтобы писать      | `#include <stdio.h>`             |
| Взял инструменты, чтобы строить | `#include <stdlib.h>` (например) |



------

### ✅ 2. `int main(int ac, char **av)`

#### Что это?

Это **главная функция программы**, её сердце.

- `main()` — точка входа, с **неё всегда начинается выполнение программы**.
- `int` — программа **возвращает число**, обычно `0`, если всё хорошо.
- `(int ac, char **av)` — это **параметры командной строки** (пока можно не вникать, они не обязательны на первых порах).

> Представь, что ты включаешь свет — и `main()` запускается первой.

📌 Без `main()` программа **не запустится вообще**.

------

### 📦 Упрощённо:

Можно писать просто:

```c
int main() {
    // ...
}
```

📌 Это работает точно так же, если ты не используешь параметры `ac` и `av`.

------

### ✅ 3. `{ ... }` — фигурные скобки

Это как **рамка**. Всё, что внутри `{ }`, — это **тело программы**.

Если `main()` — это комната, то `{}` — это **стены комнаты**. Внутри ты можешь ставить команды.

📌 Каждая функция, условие, цикл — всегда **имеют скобки**, чтобы определить **начало и конец блока**.

------

### ✅ 4. `printf("Hello, World!\n");`

#### Что это?

Команда:

> «**Покажи этот текст на экране**»

- `printf` = **print formatted** — напечатай с форматированием.
- `"Hello, World!\n"` — текст, который появится на экране.
- `\n` — **перенос строки**, как кнопка Enter.

📌 Если убрать `\n`, текст будет напечатан, но **курсор останется на той же строке**.

------

### 🧠 Как это видит компьютер:

1. Он вызывает `printf()`
2. Печатает: `Hello, World!`
3. Потом `\n` — перенос строки, курсор переходит на новую строчку

------

### ✅ 5. `return 0;`

Это как сказать:

> «📢 Всё, я закончил, всё прошло хорошо.»

- `return` — это **команда выхода** из функции `main()`.
- `0` — означает, что **не было ошибок**.

📌 Если ты хочешь сообщить об ошибке, можно вернуть другое число (`1`, `-1` и т. д.), но **`0` — это успех**.

------

### 🎯 Почему всё это важно?

| Строка               | Что делает                             |
| -------------------- | -------------------------------------- |
| `#include <stdio.h>` | Подключает библиотеку для `printf()`   |
| `int main(...)`      | Главная функция, откуда всё начинается |
| `{ }`                | Описывает, что будет выполняться       |
| `printf(...)`        | Показывает текст                       |
| `\n`                 | Переносит текст на новую строку        |
| `return 0;`          | Завершает программу без ошибок         |



------

## 📺 Как работает программа **пошагово**

1. Ты запускаешь программу
2. Компьютер ищет функцию `main()`
3. Внутри `main()` он видит `printf()` — печатает текст
4. Выполняет `return 0;` — программа завершена

------

## 🎮 Сравнение с жизнью

| В жизни               | В C                         |
| --------------------- | --------------------------- |
| Включаешь прибор      | `main()` запускается        |
| Говоришь что-то вслух | `printf()` выводит текст    |
| Нажимаешь Enter       | `\n` — новая строка         |
| Завершаешь разговор   | `return 0;` — "всё, я ушёл" |



------

## ✅ Практика

------

### 🔧 Упражнение 1: Программа с двумя строками

1. Создай файл `test.c`
2. Вставь:

```c
#include <stdio.h>

int main() {
    printf("Привет!\n");
    printf("Я учу язык C!\n");
    return 0;
}
```

1. Скомпилируй и запусти:

```bash
gcc test.c -o test
./test
```

------

### 🧪 Упражнение 2: Убери `\n` и посмотри

1. Измени строку:

```c
printf("Привет!");
printf("Я учу язык C!");
```

1. Что увидишь? — Всё появится **в одной строке**.

------

### ❌ Упражнение 3: Удали `#include <stdio.h>` — что произойдёт?

Компилятор скажет:

> “Undefined reference to `printf`” — он **не знает, где искать эту функцию**!

------

## 🧠 Итоги:

| Элемент     | Значение                       |
| ----------- | ------------------------------ |
| `#include`  | Подключает библиотеку          |
| `main()`    | Главная точка входа            |
| `{}`        | Границы функции                |
| `printf()`  | Выводит текст                  |
| `\n`        | Перенос строки                 |
| `return 0;` | Программа завершена без ошибок |